# Exclude the .env file
src/.env

# Exclude the virtual environment folder
venv/

# Exclude the service account file
src/*.json
terraform/*.json
cloudrun.json
cloudrun.json.base64

# Exclude Terraform files
.terraform/
*.tfstate
*.tfstate.backup

